_2026-06-24_

## English

Migrate kernel module configuration load path to read vpnhide_config.json directly on boot via parson, removing the sqlite3 CLI binary and reducing module zip size

## Русский

Миграция загрузки правил ядра на чтение конфигурационного файла vpnhide_config.json напрямую с помощью библиотеки parson, что позволило исключить бинарник sqlite3 и уменьшить размер архива модуля
