_2026-07-05_

## English

Replaced the port rule dialog with a full-screen editor: an icon-badge header, pink-tinted text fields matching the Ports tab, a pill-style protocol selector, and inline warning banners instead of stock Material styling — plus a Material predictive-back animation (holding the system back gesture shrinks and slides the current screen aside to reveal what's underneath) shared with the port rule, per-app/global settings, Settings, and Diagnostics detail screens

## Русский

Диалог добавления/редактирования правила порта заменён на отдельный экран: значок-бейдж в заголовке, поля ввода в розовых тонах вкладки «Порты», переключатель протокола в виде вкладок-таблеток и баннеры предупреждений вместо стандартного оформления Material — а также анимация системного жеста «назад» в стиле Material (при удержании жеста текущий экран уменьшается и сдвигается в сторону, открывая экран под ним), общая для экрана правил портов, настроек приложения/глобальных настроек, экрана настроек и деталей диагностики
