_2026-07-29_

## English

The manager app is handled correctly in both modes: it is not saved as a user exception in allowlist, remains self-targeted in blacklist, and does not trigger false empty-target warnings.

## Русский

Управляющее приложение корректно работает в обоих режимах: не сохраняется как пользовательское исключение в allowlist, остаётся self-target в blacklist, а ложное предупреждение о пустых целях не показывается.
